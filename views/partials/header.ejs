<!DOCTYPE html>
<html lang="en" class="scroll-smooth">
<head>
  <meta charset="UTF-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title><%= title %> | MyPortfolio</title>

  <!-- Google Fonts -->
  <link rel="preconnect" href="https://fonts.googleapis.com"/>
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin/>
  <link
    href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500&family=Poppins:wght@600;700&display=swap"
    rel="stylesheet"
  />

  <!-- Tailwind CDN -->
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-50 text-gray-900 font-sans">
  <!-- Navbar -->
  <nav class="bg-white shadow-sm">
    <div class="max-w-4xl mx-auto px-6 py-4 flex justify-between items-center">
      <!-- Brand -->
      <a href="/" class="text-2xl font-heading text-indigo-600 hover:text-indigo-500 transition">
        MyPortfolio
      </a>

      <!-- Show only when logged in -->
      <% if (user) { %>
        <div class="space-x-4">
          <a href="/secret-admin-123/projects"
             class="text-gray-700 hover:text-indigo-600 transition">
            Admin
          </a>
          <a href="/secret-admin-123/logout"
             class="px-3 py-1 bg-rose-500 text-white rounded-lg shadow hover:bg-rose-600 transition">
            Logout
          </a>
        </div>
      <% } %>
    </div>
  </nav>

  <!-- Main wrapper -->
  <main class="max-w-4xl mx-auto px-6 py-8">
